Icons from https://github.com/homarr-labs/dashboard-icons (Apache-2.0).
File names match the hostname label of the service (`jellyfin.svg` for
`jellyfin.reihungen.de`). Add a new icon here and reference it as `icon: "name"`
in `links.<host>.js`.
